apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-harness-website.fullname" . }}
  labels:
    {{- include "ai-harness-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-harness-website.selectorLabels" . | nindent 4 }}
